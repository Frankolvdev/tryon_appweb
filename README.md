# AppWeb — resultado bloqueado y botón de conciliación

Reemplaza únicamente:
- `src/components/generation/generation-studio.tsx`
- `src/types/generation.ts`

Muestra tokens cobrados, costo final estimado, pendiente aproximado y conecta el botón al endpoint existente de conciliación.
