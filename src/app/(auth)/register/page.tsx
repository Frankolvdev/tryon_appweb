import { AuthShell } from "@/components/auth/auth-shell";
import { RegisterForm } from "@/components/auth/register-form";
export default function RegisterPage() { return <AuthShell eyebrow="Crea sin límites" title="No imagines cómo se vería. Míralo." description="Construye un estudio personal donde cada fotografía puede convertirse en una nueva versión de ti."><RegisterForm /></AuthShell>; }
